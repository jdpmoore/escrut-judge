import { MutationTree } from 'vuex'
import { ScrutineerStateInterface } from './state'

const mutation: MutationTree<ScrutineerStateInterface> = {
  // storeCompetitorsByRoundId(
  //   state,
  //   { competitors, roundId }: { competitors: number[]; roundId: number }
  // ) {
  //   state.competitors[roundId] = competitors
  // },
}

export default mutation
